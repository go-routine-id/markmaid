//! Semantic HTML writer: `Doc -> HTML fragment` (mermaid inlined as
//! SVG via flowmaid). Implemented by the layout build agent.

pub fn render_html(_source: &str) -> String {
    todo!("layout agent")
}
